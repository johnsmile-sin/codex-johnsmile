# ================================================================
# local-stock-assistant 환경변수 예시
#
# 사용법:
#   Windows PowerShell: Copy-Item .env.example .env
#   Mac/Linux:          cp .env.example .env
#
# 이 파일(.env.example)은 GitHub에 올려도 됩니다.
# 복사본(.env)에는 실제 값을 입력하고 절대 커밋하지 마세요.
# ================================================================

# ── Supabase 설정 ─────────────────────────────────────────────
# 없으면 자동으로 Mock 모드로 실행됩니다.
# Supabase 대시보드 → Settings → API 에서 확인할 수 있습니다.
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# ── 앱 모드 설정 ──────────────────────────────────────────────
# MOCK_MODE=true  → Supabase 없이 샘플 데이터로 실행
# MOCK_MODE=false → Supabase 연결 (SUPABASE_URL, SUPABASE_ANON_KEY 필요)
MOCK_MODE=true

# ── OpenAI API (선택 사항) ────────────────────────────────────
# 뉴스 요약 AI 기능에 사용합니다.
# 없으면 Mock 텍스트로 대체됩니다.
OPENAI_API_KEY=sk-your-openai-key-here

# ── 앱 환경 ──────────────────────────────────────────────────
APP_MODE=dev
# APP_MODE=prod
